import { PropsWithChildren } from 'react';
interface TextEffectsProps {
    enabled: boolean;
}
declare const TextEffects: ({ enabled, children, }: PropsWithChildren<TextEffectsProps>) => JSX.Element;
export default TextEffects;
